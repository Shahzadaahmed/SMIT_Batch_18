const publicRoutes = ["/signup", "/login"];

const privateRoutes = [
    "/",
    "/about",
    "/contact",
    "/contact/profile",
    "/services",
    "/users",
    "/new-users"
];

export { publicRoutes, privateRoutes };